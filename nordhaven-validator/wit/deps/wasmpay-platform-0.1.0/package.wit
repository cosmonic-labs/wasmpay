package wasmpay:platform@0.1.0;

interface validation {
  record transaction {
    id: string,
    amount: s64,
    currency: string,
    status: string,
  }

  validate: func(transaction: transaction) -> bool;
}

world validate-messenger {
  import validation;
  import wasmcloud:messaging/types@0.2.0;

  export wasmcloud:messaging/handler@0.2.0;
}
